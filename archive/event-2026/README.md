# Indra Jatra 2026 interactive app archive

The original event-day pages, routes and organiser interface were archived here after the festival.
They are deliberately outside `frontend/src`, so they are preserved in Git but cannot be compiled or
accessed from the public post-event site.

The production API separately blocks organiser and admin endpoints while the event is archived.
