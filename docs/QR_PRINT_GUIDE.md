# QR Print Guide

Export seed QR targets with `npm run export:qr`. Each printed code should include the trail stop number, human label, short URL, and a visual fallback direction to the information desk.

Use high contrast, a quiet zone of at least four modules, error correction suitable for outdoor signage, and test every printed proof on iOS Safari and Android Chrome before installation.
